# ReCite AI 上线指南 - 详细步骤

## 📋 前置准备

在开始之前，请确认你已完成以下注册：
- [ ] GitHub 账号
- [ ] MongoDB Atlas 账号
- [ ] Vercel 账号
- [ ] Render 账号
- [ ] 智谱 AI API Key

---

## 第一步：配置 MongoDB 数据库

### 1.1 创建数据库集群

1. 访问 https://www.mongodb.com/cloud/atlas 并登录
2. 点击 "Build a Database"
3. 选择 "FREE" 方案，点击 "Create"
4. 填写集群信息：
   - **Cluster Name**：ReciteAI（随意）
   - **Region**：选择 Singapore（新加坡）或离你最近的区域
5. 点击 "Create Cluster"
6. 等待 2-3 分钟，集群创建完成

### 1.2 创建数据库用户

1. 在左侧菜单点击 "Database Access"
2. 点击 "Add New Database User"
3. 填写信息：
   - **Username**：reciteai（或任意用户名）
   - **Password**：创建一个强密码（**记住这个密码！**）
   - **Database User Privileges**：选择 "Read and write to any database"
4. 点击 "Create User"

### 1.3 设置 IP 白名单（允许任意访问）

1. 在左侧菜单点击 "Network Access"
2. 点击 "Add IP Address"
3. 点击 "ALLOW ACCESS FROM ANYWHERE" 按钮
4. 点击 "Confirm"

### 1.3 获取数据库连接字符串

1. 在左侧菜单点击 "Database"
2. 点击 "Connect" 按钮
3. 选择 "Connect your application"
4. 复制连接字符串，类似：
   ```
   mongodb+srv://reciteai:<password>@reciteai.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. 把 `<password>` 替换成你在 1.2 步创建的密码
6. **保存这个连接字符串！** 后面会用到

---

## 第二步：将代码上传到 GitHub

### 2.1 创建 GitHub 仓库

1. 访问 https://github.com 并登录
2. 点击右上角 "+" 图标，选择 "New repository"
3. 填写信息：
   - **Repository name**：reciteai
   - **Description**：智能英语背诵助手
   - 选择 "Public"（公开）
4. 点击 "Create repository"

### 2.2 上传代码（使用网页界面）

1. 在创建后的页面，点击 "uploading an existing file"
2. 将你电脑上的以下文件/文件夹拖拽到页面：

**需要上传的文件：**
```
ReCite Ai/
├── backend/
│   ├── config/
│   ├── controllers/
│   ├── middleware/
│   ├── models/
│   ├── routes/
│   ├── services/
│   ├── package.json
│   ├── server.js
│   └── .env.example
├── components/
├── services/
├── App.tsx
├── index.html
├── index.tsx
├── package.json
├── tsconfig.json
├── vite.config.ts
├── types.ts
├── constants.ts
├── config.ts
├── .gitignore
└── README.md
```

3. 滚动到页面底部
4. 在 "Commit changes" 框中输入：Initial commit
5. 点击 "Commit changes"（绿色按钮）

**重要！** 不要上传 `.env` 文件（包含敏感信息）

---

## 第三步：部署后端到 Render

### 3.1 在 GitHub 中创建后端代码仓库

为了方便部署，我们需要把后端代码单独放到一个仓库：

**选项 A：使用现有仓库（推荐给新手）**
继续使用上面的 reciteai 仓库即可

**选项 B：创建单独的后端仓库**
1. 再创建一个新仓库，命名为 "reciteai-backend"
2. 只上传 backend 文件夹的内容

### 3.2 在 Render 创建 Web Service

1. 访问 https://dashboard.render.com/ 并登录
2. 点击 "New" → "Web Service"
3. 选择 "Connect GitHub"
4. 找到你的仓库（reciteai 或 reciteai-backend），点击 "Connect"
5. 配置服务：
   - **Name**：reciteai-backend
   - **Environment**：Node
   - **Build Command**：`cd backend && npm install`
   - **Start Command**：`cd backend && node server.js`

6. 配置环境变量（Environment Variables）：

   点击 "Advanced" → "Add Environment Variable"，添加以下变量：

   | Key | Value |
   |-----|-------|
   | `PORT` | `3001` |
   | `MONGODB_URI` | 你在第一步复制的 MongoDB 连接字符串 |
   | `JWT_SECRET` | 随意输入一串复杂字符，如：`my-super-secret-jwt-key-2024` |
   | `JWT_EXPIRES_IN` | `7d` |
   | `ZHIPU_API_KEY` | 你的智谱 API Key |
   | `FRONTEND_URL` | 暂时填 `https://vercel.com`，部署完前端后更新 |

7. 点击 "Deploy Web Service"（底部蓝色按钮）
8. 等待 3-5 分钟，看到 "Live" 状态说明部署成功

### 3.3 获取后端 API 地址

1. 在 Render 服务页面顶部，你会看到：
   ```
   https://reciteai-backend.onrender.com
   ```
2. 点击这个地址，应该看到：
   ```json
   {"status":"ok","message":"ReCite AI Backend is running"}
   ```
3. **保存这个地址！** 后面配置前端会用到

---

## 第四步：部署前端到 Vercel

### 4.1 连接 GitHub 仓库

1. 访问 https://vercel.com/dashboard 并登录
2. 点击 "Add New" → "Project"
3. 在 "Import Git Repository" 部分，找到你的 reciteai 仓库
4. 点击 "Import"

### 4.2 配置项目

在配置页面填写：

1. **Framework Preset**：选择 "Vite"
2. **Root Directory**：保持为空 `./`
3. **Build Command**：`npm run build`
4. **Output Directory**：`dist`

### 4.3 配置环境变量

1. 找到 "Environment Variables" 部分
2. 点击 "Add New"，添加：

   | Name | Value |
   |-----|-------|
   | `VITE_API_URL` | 你在第三步获取的 Render 地址，如：`https://reciteai-backend.onrender.com` |

3. 点击 "Add"

### 4.4 部署

1. 点击 "Deploy" 按钮（底部蓝色）
2. 等待 1-2 分钟
3. 部署成功后会显示一个类似这样的地址：
   ```
   https://reciteai.vercel.app
   ```
4. **保存这个地址！** 这是你的应用网址

---

## 第五步：更新后端 CORS 设置

### 5.1 修改前端 URL

1. 回到 Render 控制台
2. 打开你的 reciteai-backend 服务
3. 点击 "Environment" 标签
4. 找到 `FRONTEND_URL` 变量
5. 点击编辑，值为你在第四步获得的 Vercel 地址：
   ```
   https://reciteai.vercel.app
   ```
6. 保存后，Render 会自动重新部署

---

## 第六步：测试你的在线应用

1. 访问你的 Vercel 地址：`https://reciteai.vercel.app`
2. 测试功能：
   - [ ] 注册新账号
   - [ ] 登录
   - [ ] 输入英文文本生成卡片
   - [ ] 查看文章库
   - [ ] 删除文章

---

## 第七步：绑定自定义域名（可选）

如果你想使用自己的域名（如 www.yourname.com）：

### 7.1 购买域名

推荐平台：
- 阿里云：https://wanwang.aliyun.com/
- 腾讯云：https://dnspod.cloud.tencent.com/
- Namecheap：https://www.namecheap.com

### 7.2 在 Vercel 添加域名

1. 在 Vercel 项目页面，点击 "Settings"
2. 点击 "Domains"
3. 输入你的域名（如 reciteai.com）
4. 按照提示配置 DNS 记录

---

## 常见问题

### Q1: Render 部署失败怎么办？

**原因**：可能是 package.json 路径不对

**解决**：
- 确保 Start Command 是：`cd backend && node server.js`
- 确保 Build Command 是：`cd backend && npm install`

### Q2: 前端无法连接后端？

**原因**：CORS 配置错误或 API 地址错误

**解决**：
1. 检查前端环境变量 `VITE_API_URL` 是否正确
2. 检查后端 `FRONTEND_URL` 是否包含前端地址
3. 在浏览器 F12 控制台查看具体错误

### Q3: MongoDB 连接失败？

**原因**：IP 白名单或密码错误

**解决**：
1. 确认在 MongoDB Atlas 设置了 "ALLOW ACCESS FROM ANYWHERE"
2. 确认连接字符串中的密码已替换

### Q4: 智谱 API 调用失败？

**原因**：API Key 错误或额度用完

**解决**：
1. 确认 API Key 正确复制
2. 访问智谱控制台查看余额

---

## 费用说明

### 免费额度

| 服务 | 免费额度 | 超出后 |
|------|---------|--------|
| MongoDB Atlas | 512 MB | 约 $9/月 |
| Render | 750 小时/月（睡眠模式） | 约 $7/月 |
| Vercel | 100 GB 带宽 | 按量计费 |

**说明**：
- 免费额度足够个人项目使用
- Render 免费版在 15 分钟无请求后会休眠，首次访问需要等待 30-60 秒唤醒
- 如果需要 24 小时运行，需要升级付费计划（约 $7/月）

---

## 下一步优化

### 1. 设置自动唤醒（防止 Render 休眠）

使用外部服务定期 ping 你的后端：
- https://healthchecks.io/
- https://uptimerobot.com/

### 2. 添加网站统计

- Google Analytics
- 百度统计

### 3. 优化加载速度

- 压缩图片
- 使用 CDN
- 代码分割

---

## 需要帮助？

如果遇到问题：
1. 查看 Render/Vercel 的部署日志
2. 检查浏览器 F12 控制台
3. 查看各平台的官方文档

祝你部署成功！🎉
